apply plugin: 'base'

repositories {
    google()
}

configurations {
    toCopy
}

dependencies {
    toCopy 'com.android.support:support-compat:28.0.0-alpha1'
}

task download(type: Copy) {
    from configurations.toCopy 
    into 'deps'
}

